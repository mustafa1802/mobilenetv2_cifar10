# Pruning Infrastructure
The following scripts provide the infrastructure to take a FP32 model, perform iterative pruning and then run a couple of fine-tuning epochs to produce a pruned fine-tuned model

# Steps to Reproduce 

`pip install -f requirements.txt`

`python main.py`
